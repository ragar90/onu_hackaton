<%= form_for @user, :html => { :class => 'form-horizontal' } do |f| %>
  <div class="control-group">
    <%= f.label :plataform_id, :class => 'control-label' %>
    <div class="controls">
      <%= f.text_field :plataform_id, :class => 'text_field' %>
    </div>
  </div>
  <div class="control-group">
    <%= f.label :phone_number, :class => 'control-label' %>
    <div class="controls">
      <%= f.text_field :phone_number, :class => 'text_field' %>
    </div>
  </div>
  <div class="control-group">
    <%= f.label :wallet, :class => 'control-label' %>
    <div class="controls">
      <%= f.text_field :wallet, :class => 'text_field' %>
    </div>
  </div>
  <div class="control-group">
    <%= f.label :use_mobile_app, :class => 'control-label' %>
    <div class="controls">
      <%= f.check_box :use_mobile_app, :class => 'check_box' %>
    </div>
  </div>
  <div class="control-group">
    <%= f.label :email, :class => 'control-label' %>
    <div class="controls">
      <%= f.text_field :email, :class => 'text_field' %>
    </div>
  </div>

  <div class="form-actions">
    <%= f.submit nil, :class => 'btn btn-primary' %>
    <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                users_path, :class => 'btn' %>
  </div>
<% end %>
